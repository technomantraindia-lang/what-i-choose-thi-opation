# Technomantra Execution Mode: Fast Edit + Runtime Optimizer V4.11.1

Fast Edit is ON. Reuse unchanged reads, batch independent discovery, make the smallest correct edit as soon as the target is known, verify only the changed area, and stop. Runtime Optimizer V4.11.2 Hard Mode is ON: full-file edit echoes are suppressed, unchanged same-file full reads are hard-blocked, safe retries are classified, pending/loading work checkpoints instead of looping, and parallel file overlap must not merge prompts.
